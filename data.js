/* Contacts live on your device only, never in this repo.
   The real data is imported once from CSV: open the app, go to the Add tab,
   and choose "Import CSV". It is then stored in your browser's localStorage
   and never uploaded anywhere. */
window.SEED_CONTACTS = [];
